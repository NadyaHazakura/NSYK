# Chromium patches

Place source patches here, for example:

`patches/0001-soul-browser-branding.patch`

The workflow applies `*.patch` files in sorted filename order.

Keep patches small and reviewable. Do not commit the entire Chromium checkout.
